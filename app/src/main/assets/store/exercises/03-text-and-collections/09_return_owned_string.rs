// Adapted from Rustlings (MIT/Apache-2.0) — rust-lang/rustlings, strings1
//
// Make this compile WITHOUT changing the function's signature, so it prints the
// favorite color. Read the error — note the two types it names.

fn current_favorite_color() -> String {
    "blue"
}

fn main() {
    println!("My current favorite color is {}", current_favorite_color());
}
