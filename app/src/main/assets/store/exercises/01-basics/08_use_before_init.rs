// Inspired by Rustlings (MIT/Apache-2.0) — github.com/rust-lang/rustlings
//
// Make this compile WITHOUT removing the `println!`. Read the error — note which
// binding it flags.

fn main() {
    let count: i32;
    println!("the count is {count}");
}
