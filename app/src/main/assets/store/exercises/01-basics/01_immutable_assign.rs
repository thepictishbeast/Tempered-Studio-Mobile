// Inspired by Rustlings (MIT/Apache-2.0) — github.com/rust-lang/rustlings
//
// Make this compile WITHOUT removing the reassignment. Read the compiler error.

fn main() {
    let count = 0;
    count = count + 1;
    println!("count is {count}");
}
