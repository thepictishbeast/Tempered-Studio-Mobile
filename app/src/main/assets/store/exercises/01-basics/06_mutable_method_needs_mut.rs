// Inspired by Rustlings (MIT/Apache-2.0) — github.com/rust-lang/rustlings
//
// Make this compile so it prints all three scores. Read the error and the
// compiler's suggestion.

fn main() {
    let scores = vec![10, 20];
    scores.push(30);
    println!("scores: {scores:?}");
}
